#ifndef __MAX_OFF_DIAGONAL_INDEXES_H
#define __MAX_OFF_DIAGONAL_INDEXES_H

#include <vector>
#include <memory>
#include "matrix.h"

namespace osoasso
{

template <typename ValueType>
std::vector<ValueType> max_off_diagonal_indexes(std::shared_ptr<matrix<ValueType>> input)
{
    std::vector<ValueType> indexes;

    return indexes;
}

}

#endif // __MAX_OFF_DIAGONAL_INDEXES_H
